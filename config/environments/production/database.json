{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-bookshelf",
      "settings": {
        "client": "mysql",
        "host": "${process.env.DATABASE_HOST || 'mysql3791.cp.blacknight.com'}",
        "port": "${process.env.DATABASE_PORT || 3306}",
        "database": "${process.env.DATABASE_NAME || 'db1335154_StrapiCMS4'}",
        "username": "${process.env.DATABASE_USERNAME || 'u1335154_strapi_'}",
        "password": "${process.env.DATABASE_PASSWORD || 'HRuaJ2Ht'}"
      },
      "options": {}
    }
  }
}
